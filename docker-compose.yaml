services:
  backend:
    build: ./marking-module
    env_file: ./marking-module/.env
    ports:
      - "${API_PORT:-8000}:${API_PORT:-8000}"

  frontend:
    build: ./react-front
    env_file: ./react-front/.env
    environment:
      - VITE_API_HOST=http://localhost
      - VITE_API_PORT=${API_PORT:-8000}
    ports:
      - "${VITE_DEV_PORT:-5173}:${VITE_DEV_PORT:-5173}"
    depends_on:
      - backend
